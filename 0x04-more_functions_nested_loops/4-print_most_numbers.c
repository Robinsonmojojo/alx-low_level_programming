#include "main.h"
#include <stdio.h>

/**
 * print_most_numbers -  prints the numbers
 * Return: 0
 */

void print_most_numbers(void)
{
	char c;

	if (c = '0', c <= '9', c++)
	{
		if (!(c == '2' || c == '4'))
			putchar(c);
	}
	putchar('\n');
}
