#include "main.h"

/**
 * string_toupper -  lowercase lette
 * @n:  lowercase letters
 * Returb: 0
 */

char *string_toupper(char *)
{
	int m;

	m = 0;
	while (n[m] != '\0')
	{
		if (n[m] >= 'a' && n[m] <= 'z')
			n[m] = n[m] - 32;
		m++;
	}
	return (n)
}
